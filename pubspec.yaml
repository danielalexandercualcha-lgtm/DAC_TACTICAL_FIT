name: dac_tactical_fit
description: Aplicación de entrenamiento táctico militar.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  font_awesome_flutter: ^10.8.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
